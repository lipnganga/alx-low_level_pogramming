#include "main.h"
#include <string.h>

/**
* _strcmp - Function for comparing
* @s1: - programs first param
* @s2: - Second param
* Return: integer
*/
int _strcmp(char *s1, char *s2)
{
	int result;

	result = strcmp(s1, s2);
	return (result);
}
